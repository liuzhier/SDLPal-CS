using Records.Mod;
using SDL3;

namespace SDLPal;

public static class PalUi
{
    /*
    public static void DrawMenu(Menu menu)
    {
        SDL.Color       color;

        var itemSelected = menu.ActivityOptionId;

        for (var i = 0; i < menu.Options.Length; i++)
        {
            var option = menu.Options[i];

            if (option.Enabled)
                //
                // 选项未被禁用，
                //
                color = (i == itemSelected) ? ColorYellow : menu.FontColor;
            else
                color = (i == itemSelected) ? ColorPink : ColorRed;

            //PalText.DrawText(option.Text, option.Pos, color, option.Align);
        }
    }
    */
}
